<UserControl x:Class="PlayniteAchievements.Views.ScoreInfoDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             MinWidth="640"
             MinHeight="650"
             d:DesignHeight="700"
             d:DesignWidth="680"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PlayniteAchievements;component/Resources/AchievementResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ScoreInfoTierIconStyle" TargetType="Image" BasedOn="{StaticResource RarityIcon}">
                <Setter Property="Width" Value="34"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="Effect" Value="{StaticResource BadgeShadow}"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="ScoreInfoTierArrowStyle" TargetType="TextBlock" BasedOn="{StaticResource PlayAch.TextBlock.BaseStyle}">
                <Setter Property="Text" Value="&#xE76C;"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{DynamicResource PlayAch.Brush.Text}"/>
                <Setter Property="Opacity" Value="0.58"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
                <Setter Property="Margin" Value="0,22,0,0"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{DynamicResource LOCPlayAch_Score_Info_Title}"
                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                   Margin="0,0,0,14"/>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0"
                        Grid.Column="0"
                        BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                        BorderThickness="{StaticResource PlayAch.Thickness.Border}"
                        Background="{DynamicResource PlayAch.Brush.Surface}"
                        CornerRadius="{StaticResource PlayAch.Radius.Card}"
                        Padding="14"
                        Margin="0,0,8,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Score_Info_CollectionTitle}"
                                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                                   Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}"/>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Score_Info_CollectionBody}"
                                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                                   Opacity="0.82"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="0"
                        Grid.Column="1"
                        BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                        BorderThickness="{StaticResource PlayAch.Thickness.Border}"
                        Background="{DynamicResource PlayAch.Brush.Surface}"
                        CornerRadius="{StaticResource PlayAch.Radius.Card}"
                        Padding="14"
                        Margin="8,0,0,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Score_Info_PrestigeTitle}"
                                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                                   Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}"/>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Score_Info_PrestigeBody}"
                                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                                   Opacity="0.82"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                        BorderThickness="{StaticResource PlayAch.Thickness.Border}"
                        Background="{DynamicResource PlayAch.Brush.Surface}"
                        CornerRadius="{StaticResource PlayAch.Radius.Card}"
                        Padding="14">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Score_Info_LevelsTitle}"
                                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                                   Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}"/>
                        <Grid Margin="0,4,0,18"
                              MinHeight="116">
                            <Border Height="2"
                                    Margin="48,28,48,0"
                                    VerticalAlignment="Top"
                                    Background="{DynamicResource PlayAch.Brush.Border}"
                                    Opacity="{StaticResource PlayAch.Opacity.Disabled}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"
                                            HorizontalAlignment="Center"
                                            MinWidth="88"
                                            Margin="{StaticResource PlayAch.Thickness.Bottom.Xs}">
                                    <Grid Width="56" Height="56" HorizontalAlignment="Center" Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}">
                                        <Border CornerRadius="28"
                                                Background="{StaticResource PlayAch.Brush.Overlay.Tint.12}"
                                                BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                                                BorderThickness="{StaticResource PlayAch.Thickness.Border}"/>
                                        <Image Source="{StaticResource BadgeBronzeTriangle}"
                                               Style="{StaticResource ScoreInfoTierIconStyle}"/>
                                    </Grid>
                                    <TextBlock Text="{DynamicResource LOCPlayAch_Trophy_Bronze}"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontWeight="SemiBold"
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="V-I"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontSize="11"
                                               Opacity="0.68"
                                               TextAlignment="Center"
                                               LineHeight="14"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1"
                                           Style="{StaticResource ScoreInfoTierArrowStyle}"/>

                                <StackPanel Grid.Column="2"
                                            HorizontalAlignment="Center"
                                            MinWidth="88"
                                            Margin="{StaticResource PlayAch.Thickness.Bottom.Xs}">
                                    <Grid Width="56" Height="56" HorizontalAlignment="Center" Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}">
                                        <Border CornerRadius="28"
                                                Background="{StaticResource PlayAch.Brush.Overlay.Tint.12}"
                                                BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                                                BorderThickness="{StaticResource PlayAch.Thickness.Border}"/>
                                        <Image Source="{StaticResource BadgeSilverSquare}"
                                               Style="{StaticResource ScoreInfoTierIconStyle}"/>
                                    </Grid>
                                    <TextBlock Text="{DynamicResource LOCPlayAch_Trophy_Silver}"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontWeight="SemiBold"
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="V-I"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontSize="11"
                                               Opacity="0.68"
                                               TextAlignment="Center"
                                               LineHeight="14"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="3"
                                           Style="{StaticResource ScoreInfoTierArrowStyle}"/>

                                <StackPanel Grid.Column="4"
                                            HorizontalAlignment="Center"
                                            MinWidth="88"
                                            Margin="{StaticResource PlayAch.Thickness.Bottom.Xs}">
                                    <Grid Width="56" Height="56" HorizontalAlignment="Center" Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}">
                                        <Border CornerRadius="28"
                                                Background="{StaticResource PlayAch.Brush.Overlay.Tint.12}"
                                                BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                                                BorderThickness="{StaticResource PlayAch.Thickness.Border}"/>
                                        <Image Source="{StaticResource BadgeGoldPentagon}"
                                               Style="{StaticResource ScoreInfoTierIconStyle}"/>
                                    </Grid>
                                    <TextBlock Text="{DynamicResource LOCPlayAch_Trophy_Gold}"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontWeight="SemiBold"
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="V-I"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontSize="11"
                                               Opacity="0.68"
                                               TextAlignment="Center"
                                               LineHeight="14"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="5"
                                           Style="{StaticResource ScoreInfoTierArrowStyle}"/>

                                <StackPanel Grid.Column="6"
                                            HorizontalAlignment="Center"
                                            MinWidth="88"
                                            Margin="{StaticResource PlayAch.Thickness.Bottom.Xs}">
                                    <Grid Width="56" Height="56" HorizontalAlignment="Center" Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}">
                                        <Border CornerRadius="28"
                                                Background="{StaticResource PlayAch.Brush.Overlay.Tint.12}"
                                                BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                                                BorderThickness="{StaticResource PlayAch.Thickness.Border}"/>
                                        <Image Source="{StaticResource BadgePlatinumHexagon}"
                                               Style="{StaticResource ScoreInfoTierIconStyle}"/>
                                    </Grid>
                                    <TextBlock Text="{DynamicResource LOCPlayAch_Trophy_Platinum}"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontWeight="SemiBold"
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="V-I"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontSize="11"
                                               Opacity="0.68"
                                               TextAlignment="Center"
                                               LineHeight="14"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="7"
                                           Style="{StaticResource ScoreInfoTierArrowStyle}"/>

                                <StackPanel Grid.Column="8"
                                            HorizontalAlignment="Center"
                                            MinWidth="88"
                                            Margin="{StaticResource PlayAch.Thickness.Bottom.Xs}">
                                    <Grid Width="56" Height="56" HorizontalAlignment="Center" Margin="{StaticResource PlayAch.Thickness.Bottom.Sm}">
                                        <Border CornerRadius="28"
                                                Background="{StaticResource PlayAch.Brush.Overlay.Tint.12}"
                                                BorderBrush="{DynamicResource PlayAch.Brush.Border}"
                                                BorderThickness="{StaticResource PlayAch.Thickness.Border}"/>
                                        <Image Source="{DynamicResource BadgeCompletedGame}"
                                               Style="{StaticResource ScoreInfoTierIconStyle}"/>
                                    </Grid>
                                    <TextBlock Text="{DynamicResource LOCPlayAch_Score_Tier_Master}"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontWeight="SemiBold"
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="V-I"
                                               Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                               Foreground="{DynamicResource PlayAch.Brush.Text}"
                                               FontSize="11"
                                               Opacity="0.68"
                                               TextAlignment="Center"
                                               LineHeight="14"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Score_Info_LevelsBody}"
                                   Style="{DynamicResource PlayAch.TextBlock.BaseStyle}"
                                   Foreground="{DynamicResource PlayAch.Brush.Text}"
                                   Opacity="0.82"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="{DynamicResource LOCPlayAch_Button_Ok}"
                    Click="CloseButton_Click"
                    Padding="{StaticResource PlayAch.Thickness.ButtonPadding.Wide}"
                    MinWidth="84"
                    IsDefault="True"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</UserControl>
