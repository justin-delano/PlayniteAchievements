<UserControl x:Class="PlayniteAchievements.Views.OverviewHostControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="350">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PlayniteAchievements;component/Resources/CommonResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <ContentPresenter x:Name="PART_Content" />

        <!-- Lightweight placeholder shown until the real overview is created -->
        <Border x:Name="PART_Loading"
                Background="Transparent"
                Padding="{StaticResource PlayAch.Thickness.CardPadding}">
            <StackPanel>
                <TextBlock Text="Loading…"
                           FontWeight="SemiBold"
                           Margin="{StaticResource PlayAch.Thickness.Bottom.Compact}" />
                <ProgressBar Height="4"
                             IsIndeterminate="True" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

