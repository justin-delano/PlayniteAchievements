<UserControl x:Class="PlayniteAchievements.Views.Controls.PreviewProgressBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shared="clr-namespace:PlayniteAchievements.Views.ThemeIntegration.Desktop.Shared"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PlayniteAchievements;component/Resources/AchievementTemplates.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/PlayniteAchievements;component/Resources/RarityBadges.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 1: Progress bar with percentage overlay -->
        <Grid Grid.Row="0">
            <ProgressBar Value="{Binding ProgressPercentage, ElementName=Root, Mode=OneWay}"
                         Maximum="100"
                         Style="{StaticResource GameProgressBar}"
                         Height="22"/>
            <TextBlock Text="{Binding ProgressPercentage, ElementName=Root, Mode=OneWay, StringFormat={}{0:F0}%}"
                       HorizontalAlignment="Left"
                       Margin="6,0,0,0"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       FontSize="{DynamicResource FontSizeSmall}"
                       Foreground="White"/>
        </Grid>

        <!-- Row 2: Rarity badges -->
        <shared:RarityBadgesControl Grid.Row="1"
                                    Margin="0,6,0,0"
                                    DataContext="{Binding MockThemeData, ElementName=Root}"/>
    </Grid>
</UserControl>
