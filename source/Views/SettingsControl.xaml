<!-- SettingsControl.xaml -->
<UserControl x:Class="PlayniteAchievements.Views.SettingsControl"
             x:Name="SettingsControlRoot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:PlayniteAchievements.Views.Converters"
             xmlns:base="clr-namespace:PlayniteAchievements.Views.ThemeIntegration.Base"
             xmlns:controls="clr-namespace:PlayniteAchievements.Views.Controls"
             xmlns:modern="clr-namespace:PlayniteAchievements.Views.ThemeIntegration.Modern"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="700">

    <UserControl.Resources>
        <ResourceDictionary>
            <conv:NullableDoubleTextConverter x:Key="NullableDoubleTextConverter"/>

            <Style x:Key="PreviewThemeControlStyle"
                   TargetType="{x:Type base:ThemeControlBase}">
                <Setter Property="SettingsOverride"
                        Value="{Binding DataContext, ElementName=SettingsControlRoot}"/>
            </Style>

            <!-- Disable Steam auth buttons while busy -->
            <Style x:Key="SteamAuthActionButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding SteamAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Disable GOG auth buttons while busy -->
            <Style x:Key="GogAuthActionButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding GogAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Disable Epic auth buttons while busy -->
            <Style x:Key="EpicAuthActionButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding EpicAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Disable PSN auth buttons while busy -->
            <Style x:Key="PsnAuthActionButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding PsnAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Disable Xbox auth buttons while busy -->
            <Style x:Key="XboxAuthActionButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding XboxAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Disable Exophase auth buttons while busy -->
            <Style x:Key="ExophaseAuthActionButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ExophaseAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Section header style -->
            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="Margin" Value="0,0,0,12"/>
                <Setter Property="Opacity" Value="1"/>
            </Style>

            <!-- Subsection header style -->
            <Style x:Key="SubSectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="Opacity" Value="0.96"/>
            </Style>

            <Style x:Key="SectionAccentRuleStyle" TargetType="Border">
                <Setter Property="Height" Value="2"/>
                <Setter Property="Width" Value="44"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="CornerRadius" Value="1"/>
                <Setter Property="Background" Value="{DynamicResource HighlightGlyphBrush}"/>
                <Setter Property="Margin" Value="0,0,0,14"/>
            </Style>

            <Style x:Key="ContextLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{DynamicResource HighlightGlyphBrush}"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>

            <Style x:Key="SettingGroupCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="14"/>
                <Setter Property="Margin" Value="0,0,0,14"/>
                <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <!-- Accent text style -->
            <Style x:Key="AccentTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="{DynamicResource FontSize}"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Foreground" Value="#4CC9F0"/>
            </Style>

            <!-- Modern button style -->
            <Style x:Key="ModernActionButton" TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Padding" Value="18,8"/>
                <Setter Property="MinWidth" Value="120"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.85"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Opacity" Value="0.7"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Section container style with shadow -->
            <Style x:Key="SectionBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="18"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <!-- Setting row style -->
            <Style x:Key="SettingRowStyle" TargetType="Grid">
                <Setter Property="Margin" Value="0,0,0,14"/>
            </Style>

            <Style x:Key="MigrationModeToggleStyle"
                   TargetType="ToggleButton"
                   BasedOn="{StaticResource {x:Type ToggleButton}}">
                <Setter Property="MinWidth" Value="88"/>
                <Setter Property="Padding" Value="12,4"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Content" Value="{DynamicResource LOCPlayAch_ThemeMigration_ModeLegacy}"/>
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter Property="Content" Value="{DynamicResource LOCPlayAch_ThemeMigration_ModeModern}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="MigrationChoiceButtonBaseStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Padding" Value="12,5"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="Background" Value="#32000000"/>
                <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Chrome"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Chrome" Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MigrationChoiceLegacyButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource MigrationChoiceButtonBaseStyle}">
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="True"/>
                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#42000000"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                    </MultiDataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="True"/>
                            <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#54000000"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                    </MultiDataTrigger>
                    <DataTrigger Binding="{Binding IsModern}" Value="False">
                        <Setter Property="Background" Value="{DynamicResource HighlightGlyphBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="False"/>
                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#FF37A7C8"/>
                        <Setter Property="BorderBrush" Value="#FF37A7C8"/>
                        <Setter Property="Foreground" Value="White"/>
                    </MultiDataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="False"/>
                            <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#FF2D8FAB"/>
                        <Setter Property="BorderBrush" Value="#FF2D8FAB"/>
                        <Setter Property="Foreground" Value="White"/>
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="MigrationChoiceModernButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource MigrationChoiceButtonBaseStyle}">
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="False"/>
                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#42000000"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                    </MultiDataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="False"/>
                            <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#54000000"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                    </MultiDataTrigger>
                    <DataTrigger Binding="{Binding IsModern}" Value="True">
                        <Setter Property="Background" Value="{DynamicResource HighlightGlyphBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                        <Setter Property="Foreground" Value="White"/>
                    </DataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="True"/>
                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#FF37A7C8"/>
                        <Setter Property="BorderBrush" Value="#FF37A7C8"/>
                        <Setter Property="Foreground" Value="White"/>
                    </MultiDataTrigger>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsModern}" Value="True"/>
                            <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#FF2D8FAB"/>
                        <Setter Property="BorderBrush" Value="#FF2D8FAB"/>
                        <Setter Property="Foreground" Value="White"/>
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="MigrationSwitchToggleStyle"
                   TargetType="ToggleButton"
                   BasedOn="{StaticResource {x:Type ToggleButton}}">
                <Setter Property="Width" Value="46"/>
                <Setter Property="Height" Value="22"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="#22000000"/>
                <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Track"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="11">
                                <Grid Margin="2">
                                    <Ellipse x:Name="Thumb"
                                             Width="16"
                                             Height="16"
                                             Fill="{DynamicResource TextBrush}"
                                             HorizontalAlignment="Left"
                                             VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="{DynamicResource HighlightGlyphBrush}"/>
                                    <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource HighlightGlyphBrush}"/>
                                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Track" Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Muted text style -->
            <Style x:Key="MutedTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="Opacity" Value="0.6"/>
                <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="24,20,24,24">
        <TabControl x:Name="SettingsTabControl" SelectionChanged="TabControl_SelectionChanged" BorderThickness="0">
            <!-- ================== TAB: GENERAL SETTINGS ================== -->
            <TabItem x:Name="GeneralTab" Header="{DynamicResource LOCPlayAch_Settings_TabGeneral}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Settings Header -->
                        <Border Style="{StaticResource SectionBorderStyle}"
                                Background="{DynamicResource PanelBackgroundBrush}"
                                BorderBrush="{DynamicResource NormalBorderBrush}">
                            <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Description}"
                                       Style="{StaticResource SectionHeaderStyle}"
                                       Margin="0"/>
                        </Border>

                        <!-- ================== LANGUAGE ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Language}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_GlobalLanguage}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,2"/>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_GlobalLanguageHint}"
                                                   Style="{StaticResource MutedTextStyle}"/>
                                    </StackPanel>

                                    <ComboBox Grid.Column="1"
                                              Width="200"
                                              SelectedValuePath="Tag"
                                              SelectedValue="{Binding Persisted.GlobalLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="English" Tag="english"/>
                                        <ComboBoxItem Content="German (Deutsch)" Tag="german"/>
                                        <ComboBoxItem Content="French (Français)" Tag="french"/>
                                        <ComboBoxItem Content="Spanish (Español)" Tag="spanish"/>
                                        <ComboBoxItem Content="Italian (Italiano)" Tag="italian"/>
                                        <ComboBoxItem Content="Portuguese" Tag="portuguese"/>
                                        <ComboBoxItem Content="Brazilian Portuguese" Tag="brazilian"/>
                                        <ComboBoxItem Content="Russian (Русский)" Tag="russian"/>
                                        <ComboBoxItem Content="Polish (Polski)" Tag="polish"/>
                                        <ComboBoxItem Content="Dutch (Nederlands)" Tag="dutch"/>
                                        <ComboBoxItem Content="Swedish (Svenska)" Tag="swedish"/>
                                        <ComboBoxItem Content="Finnish (Suomi)" Tag="finnish"/>
                                        <ComboBoxItem Content="Danish (Dansk)" Tag="danish"/>
                                        <ComboBoxItem Content="Norwegian (Norsk)" Tag="norwegian"/>
                                        <ComboBoxItem Content="Hungarian (Magyar)" Tag="hungarian"/>
                                        <ComboBoxItem Content="Czech (Čeština)" Tag="czech"/>
                                        <ComboBoxItem Content="Romanian (Română)" Tag="romanian"/>
                                        <ComboBoxItem Content="Turkish (Türkçe)" Tag="turkish"/>
                                        <ComboBoxItem Content="Greek (Ελληνικά)" Tag="greek"/>
                                        <ComboBoxItem Content="Bulgarian (Български)" Tag="bulgarian"/>
                                        <ComboBoxItem Content="Ukrainian (Українська)" Tag="ukrainian"/>
                                        <ComboBoxItem Content="Thai (ไทย)" Tag="thai"/>
                                        <ComboBoxItem Content="Vietnamese (Tiếng Việt)" Tag="vietnamese"/>
                                        <ComboBoxItem Content="Japanese (日本語)" Tag="japanese"/>
                                        <ComboBoxItem Content="Korean (한국어)" Tag="koreana"/>
                                        <ComboBoxItem Content="Simplified Chinese (简体中文)" Tag="schinese"/>
                                        <ComboBoxItem Content="Traditional Chinese (繁體中文)" Tag="tchinese"/>
                                        <ComboBoxItem Content="Arabic (العربية)" Tag="arabic"/>
                                    </ComboBox>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- ================== SYNC & UPDATES ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Section_SyncUpdates}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <!-- Periodic Updates -->
                                <Grid Style="{StaticResource SettingRowStyle}" Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnablePeriodicUpdates}"
                                                          IsChecked="{Binding Persisted.EnablePeriodicUpdates}"
                                                  VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_HoursBetweenUpdates}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>
                                            <TextBox Text="{Binding Persisted.PeriodicUpdateHours, UpdateSourceTrigger=PropertyChanged}"
                                                 Width="50"
                                                     IsEnabled="{Binding Persisted.EnablePeriodicUpdates}"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel Margin="0,0,0,16">
                                    <CheckBox Content="{DynamicResource LOCPlayAch_Settings_AutoExcludeHiddenGames}"
                                              IsChecked="{Binding Persisted.AutoExcludeHiddenGames}"
                                              Margin="0,0,0,4"/>
                                    <TextBlock Text="{DynamicResource LOCPlayAch_Settings_AutoExcludeHiddenGames_Description}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="18,0,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <!-- Recent Refresh -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_RecentRefresh_Title}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,2"/>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_RecentRefresh_Description}"
                                                   Style="{StaticResource MutedTextStyle}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_RecentRefresh_RecentGames}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>
                                            <TextBox Text="{Binding Persisted.RecentRefreshGamesCount, UpdateSourceTrigger=PropertyChanged}"
                                                 Width="50"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Include Unplayed Games -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                            <CheckBox Content="{DynamicResource LOCPlayAch_IncludeUnplayedGames}"
                                                          IsChecked="{Binding Persisted.IncludeUnplayedGames}"
                                                  VerticalAlignment="Center"/>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_IncludeUnplayedGames_Help}"
                                                   Style="{StaticResource MutedTextStyle}"
                                                   Margin="20,4,0,0"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Provider Execution Mode -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnableParallelProviderRefresh}"
                                                  IsChecked="{Binding Persisted.EnableParallelProviderRefresh}"
                                                  VerticalAlignment="Center"/>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_EnableParallelProviderRefresh_Help}"
                                                   Style="{StaticResource MutedTextStyle}"
                                                   Margin="20,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- ================== NOTIFICATIONS ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Section_Notifications}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <StackPanel>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnableNotifications}"
                                                  IsChecked="{Binding Persisted.EnableNotifications}"
                                              Margin="0,0,0,10"/>
                                    <CheckBox Content="{DynamicResource LOCPlayAch_Settings_NotifyPeriodicUpdates}"
                                                  IsChecked="{Binding Persisted.NotifyPeriodicUpdates}"
                                              Margin="18,0,0,10"
                                                  IsEnabled="{Binding Persisted.EnableNotifications}"/>
                                    <CheckBox Content="{DynamicResource LOCPlayAch_Settings_NotifyOnRebuild}"
                                                  IsChecked="{Binding Persisted.NotifyOnRebuild}"
                                              Margin="18,0,0,0"
                                                  IsEnabled="{Binding Persisted.EnableNotifications}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- ================== TAGGING ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_TaggingHeader}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_TaggingDescription}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           TextWrapping="Wrap"/>

                                <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnableTagging}"
                                          IsChecked="{Binding Persisted.TaggingSettings.EnableTagging, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Margin="0,0,0,12"/>

                                <!-- Tag Configuration Grid - only visible when tagging is enabled -->
                                <StackPanel Margin="18,0,0,0"
                                            Visibility="{Binding Persisted.TaggingSettings.EnableTagging, Converter={StaticResource BooleanToVisibilityConverter}}">

                                    <TextBlock Text="{DynamicResource LOCPlayAch_Settings_TaggingSection}"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextBrush}"
                                               Margin="0,0,0,12"/>

                                    <!-- Tag List -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Has Achievements Tag -->
                                        <CheckBox Grid.Row="0" Grid.Column="0"
                                                  IsChecked="{Binding Persisted.TaggingSettings.HasAchievementsConfig.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,6"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1"
                                                   Text="{DynamicResource LOCPlayAch_Tagging_HasAchievements}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,6"/>
                                        <TextBox Grid.Row="0" Grid.Column="2"
                                                 x:Name="HasAchievementsTagTextBox"
                                                 Text="{Binding Persisted.TaggingSettings.HasAchievementsConfig.DisplayName, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                                 Margin="0,0,0,6"/>

                                        <!-- In Progress Tag -->
                                        <CheckBox Grid.Row="1" Grid.Column="0"
                                                  IsChecked="{Binding Persisted.TaggingSettings.InProgressConfig.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,6"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1"
                                                   Text="{DynamicResource LOCPlayAch_Tagging_InProgress}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,6"/>
                                        <TextBox Grid.Row="1" Grid.Column="2"
                                                 x:Name="InProgressTagTextBox"
                                                 Text="{Binding Persisted.TaggingSettings.InProgressConfig.DisplayName, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                                 Margin="0,0,0,6"/>

                                        <!-- Completed Tag -->
                                        <CheckBox Grid.Row="2" Grid.Column="0"
                                                  IsChecked="{Binding Persisted.TaggingSettings.CompletedConfig.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,6"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1"
                                                   Text="{DynamicResource LOCPlayAch_Tagging_Completed}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,6"/>
                                        <TextBox Grid.Row="2" Grid.Column="2"
                                                 x:Name="CompletedTagTextBox"
                                                 Text="{Binding Persisted.TaggingSettings.CompletedConfig.DisplayName, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                                 Margin="0,0,0,6"/>

                                        <!-- No Achievements Tag -->
                                        <CheckBox Grid.Row="3" Grid.Column="0"
                                                  IsChecked="{Binding Persisted.TaggingSettings.NoAchievementsConfig.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,6"/>
                                        <TextBlock Grid.Row="3" Grid.Column="1"
                                                   Text="{DynamicResource LOCPlayAch_Tagging_NoAchievements}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,6"/>
                                        <TextBox Grid.Row="3" Grid.Column="2"
                                                 x:Name="NoAchievementsTagTextBox"
                                                 Text="{Binding Persisted.TaggingSettings.NoAchievementsConfig.DisplayName, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                                 Margin="0,0,0,6"/>

                                        <!-- Excluded Tag -->
                                        <CheckBox Grid.Row="4" Grid.Column="0"
                                                  IsChecked="{Binding Persisted.TaggingSettings.ExcludedConfig.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,6"/>
                                        <TextBlock Grid.Row="4" Grid.Column="1"
                                                   Text="{DynamicResource LOCPlayAch_Tagging_Excluded}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,6"/>
                                        <TextBox Grid.Row="4" Grid.Column="2"
                                                 x:Name="ExcludedTagTextBox"
                                                 Text="{Binding Persisted.TaggingSettings.ExcludedConfig.DisplayName, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                                 Margin="0,0,0,6"/>

                                        <!-- Excluded from Summaries Tag -->
                                        <CheckBox Grid.Row="5" Grid.Column="0"
                                                  IsChecked="{Binding Persisted.TaggingSettings.ExcludedFromSummariesConfig.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,0"/>
                                        <TextBlock Grid.Row="5" Grid.Column="1"
                                                   Text="{DynamicResource LOCPlayAch_Tagging_ExcludedFromSummaries}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,0"/>
                                        <TextBox Grid.Row="5" Grid.Column="2"
                                                 x:Name="ExcludedFromSummariesTagTextBox"
                                                 Text="{Binding Persisted.TaggingSettings.ExcludedFromSummariesConfig.DisplayName, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                                 Margin="0,0,0,0"/>
                                    </Grid>

                                    <!-- Completion Status -->
                                    <CheckBox Content="{DynamicResource LOCPlayAch_Settings_SetCompletionStatus}"
                                              IsChecked="{Binding Persisted.TaggingSettings.SetCompletionStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              Margin="0,12,0,8"/>

                                    <!-- Action Buttons -->
                                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                        <Button Content="{DynamicResource LOCPlayAch_Settings_ApplyAndSync}"
                                                Click="ApplyAndSync_Click"
                                                Margin="0,0,8,0"
                                                Padding="12,6"/>
                                        <Button Content="{DynamicResource LOCPlayAch_Settings_RemoveAllTags}"
                                                Click="RemoveAllTags_Click"
                                                Padding="12,6"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- ================== THEME INTEGRATION ================== -->
                        <!-- <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Theme Integration"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16">
                                    PlayniteAchievements is the single source for both Desktop and Fullscreen themes.
                                    Both modern and SuccessStory-compatible properties are always populated so themes can bind to either surface.
                                </TextBlock>

                                 <Separator Margin="0,16,0,16" />

                                <TextBlock Text="Modern PlayniteAchievements theme integration"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8" />

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,0">
                                    Modern controls and properties (prefixed differently) are always available to themes.
                                    Theme authors should use the modern PlayniteAchievements prefix for new themes.
                                </TextBlock>
                            </StackPanel>
                        </Border> -->

                        <!-- ================== DATA MANAGEMENT ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Cache_Title}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_Cache_Description}"
                                               Style="{StaticResource MutedTextStyle}"
                                               VerticalAlignment="Center"/>

                                    <Button Grid.Row="0" Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_WipeCache_All}"
                                            Click="WipeCache_Click"
                                            Padding="14,6"
                                            Margin="16,0,0,0"
                                            MinWidth="90"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_ImageCache_Description}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,10,0,0"
                                               VerticalAlignment="Top"/>

                                    <Button Grid.Row="1" Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_ImageCache_Clear}"
                                            Click="ClearImageCache_Click"
                                            Padding="14,6"
                                            Margin="16,10,0,0"
                                            MinWidth="90"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_ResetFirstTimeSetup}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,10,0,0"
                                               VerticalAlignment="Top"/>

                                    <Button Grid.Row="2" Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_ResetFirstTimeSetupButton}"
                                            Click="ResetFirstTimeSetup_Click"
                                            Padding="14,6"
                                            Margin="16,10,0,0"
                                            MinWidth="90"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_ExportDatabase}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,10,0,0"
                                               VerticalAlignment="Top"/>

                                    <Button Grid.Row="3" Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_ExportDatabaseButton}"
                                            Click="ExportDatabase_Click"
                                            Padding="14,6"
                                            Margin="16,10,0,0"
                                            MinWidth="90"/>

                                    <TextBlock Grid.Row="4" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_OpenDataFolder}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,10,0,0"
                                               VerticalAlignment="Top"/>

                                    <Button Grid.Row="4" Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_OpenDataFolderButton}"
                                            Click="OpenDataFolder_Click"
                                            Padding="14,6"
                                            Margin="16,10,0,0"
                                            MinWidth="90"/>

                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: DISPLAY ================== -->
            <TabItem x:Name="DisplayTab" Header="{DynamicResource LOCPlayAch_Settings_TabDisplay}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <Border Style="{StaticResource SectionBorderStyle}"
                                Background="{DynamicResource PanelBackgroundBrush}"
                                BorderBrush="{DynamicResource NormalBorderBrush}">
                            <TextBlock Text="Display Options"
                                       Style="{StaticResource SectionHeaderStyle}"
                                       Margin="0"/>
                        </Border>

                        <!-- ================== DISPLAY OPTIONS SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Display_GeneralSection}"
                                                   Style="{StaticResource SubSectionHeaderStyle}"
                                                   Margin="0,0,0,10"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnableCompactGridMode}"
                                                  IsChecked="{Binding Persisted.EnableCompactGridMode}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowTopMenuBarButton}"
                                                  IsChecked="{Binding Persisted.ShowTopMenuBarButton}"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Display_SidebarSection}"
                                                   Style="{StaticResource SubSectionHeaderStyle}"
                                                   Margin="0,0,0,10"/>

                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowSidebarGamesPieChart}"
                                                  IsChecked="{Binding Persisted.ShowSidebarGamesPieChart}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowSidebarProviderPieChart}"
                                                  IsChecked="{Binding Persisted.ShowSidebarProviderPieChart}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowSidebarRarityPieChart}"
                                                  IsChecked="{Binding Persisted.ShowSidebarRarityPieChart}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowSidebarTrophyPieChart}"
                                                  IsChecked="{Binding Persisted.ShowSidebarTrophyPieChart}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowSidebarBarCharts}"
                                                  IsChecked="{Binding Persisted.ShowSidebarBarCharts}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowGamesWithNoUnlocks}"
                                                  IsChecked="{Binding Persisted.ShowGamesWithNoUnlocks}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowUnplayedGames}"
                                                  IsChecked="{Binding Persisted.ShowUnplayedGames}"
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_UseCoverImages}"
                                                  IsChecked="{Binding Persisted.UseCoverImages}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== ACHIEVEMENTS SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Display_AchievementsSection}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>

                                <!-- Unlocked Achievement Visibility -->
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_UnlockedAchievements}"
                                                   Style="{StaticResource SubSectionHeaderStyle}"
                                                   Margin="0,0,0,8"/>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementDataGridControl
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding UnlockedPreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            PreviewMaxHeightOverride="122"
                                            Margin="0,0,0,12"/>
                                        <TextBlock Text="SETTINGS"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowRarityGlow}"
                                                  IsChecked="{Binding Persisted.ShowRarityGlow}"
                                                  VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- Locked Achievement Visibility -->
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_LockedAchievements}"
                                                   Style="{StaticResource SubSectionHeaderStyle}"
                                                   Margin="0,0,0,4"/>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_LockedAchievements_Description}"
                                                   Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8"/>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementDataGridControl
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding LockedPreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            PreviewMaxHeightOverride="122"
                                            Margin="0,0,0,12"/>
                                        <TextBlock Text="SETTINGS"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_RevealLockedIcon}"
                                                  IsChecked="{Binding Persisted.ShowLockedIcon}"
                                                  VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- Hidden Achievement Visibility -->
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_HiddenAchievements}"
                                                   Style="{StaticResource SubSectionHeaderStyle}"
                                                   Margin="0,0,0,4"/>
                                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_HiddenAchievements_Description}"
                                                   Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8"/>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementDataGridControl
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding HiddenPreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            PreviewMaxHeightOverride="122"
                                            Margin="0,0,0,12"/>
                                        <TextBlock Text="SETTINGS"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <StackPanel>
                                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_RevealHiddenIcon}"
                                                      IsChecked="{Binding Persisted.ShowHiddenIcon}" Margin="0,0,0,6"/>
                                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_RevealHiddenTitle}"
                                                      IsChecked="{Binding Persisted.ShowHiddenTitle}" Margin="0,0,0,6"/>
                                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_RevealHiddenDescription}"
                                                      IsChecked="{Binding Persisted.ShowHiddenDescription}" Margin="0,0,0,6"/>
                                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowHiddenSuffix}"
                                                      IsChecked="{Binding Persisted.ShowHiddenSuffix}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== COMPACT LIST PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_CompactListPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementCompactListControl
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            IconSize="48"
                                            Margin="0,0,0,12"/>
                                        <TextBlock Text="SETTINGS"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ShowCompactListRarityBar}"
                                                  IsChecked="{Binding Persisted.ShowCompactListRarityBar}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== DATA GRID PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_AchievementDataGridPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementDataGridControl
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            PreviewMinimumMaxHeight="220"
                                            Margin="0,0,0,12"/>
                                        <TextBlock Text="SETTINGS"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource LOCPlayAch_Settings_AchievementDataGridMaxHeight}"
                                                       VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding Persisted.AchievementDataGridMaxHeight, UpdateSourceTrigger=LostFocus, Converter={StaticResource NullableDoubleTextConverter}, ConverterParameter=88}"
                                                     Width="80" Margin="8,0,0,0"/>
                                            <TextBlock Text="{DynamicResource LOCPlayAch_Settings_PixelsUnit}"
                                                       VerticalAlignment="Center" Margin="4,0,0,0" Opacity="0.6"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== COMPACT UNLOCKED LIST PREVIEW SECTION ================== -->
                        <!-- Disabled until this preview is needed.
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_CompactUnlockedListPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementCompactUnlockedListControl
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            IconSize="48"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                        -->

                        <!-- ================== COMPACT LOCKED LIST PREVIEW SECTION ================== -->
                        <!-- Disabled until this preview is needed.
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_CompactLockedListPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementCompactLockedListControl
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            IconSize="48"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                        -->

                        <!-- ================== PROGRESS BAR PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProgressBarPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementProgressBarControl Width="280"
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== STATS PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_StatsPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementStatsControl Width="200"
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== BUTTON PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ButtonPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <StackPanel Orientation="Horizontal">
                                            <modern:AchievementButtonControl Width="40" DisplayDetails="False" Margin="0,0,16,0"
                                                Style="{StaticResource PreviewThemeControlStyle}"
                                                ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                            <modern:AchievementButtonControl Width="200" DisplayDetails="True"
                                                Style="{StaticResource PreviewThemeControlStyle}"
                                                ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== VIEW ITEM PREVIEW SECTION ================== -->
                        <!-- Disabled until this preview is needed.
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ViewItemPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <StackPanel Orientation="Horizontal">
                                            <modern:AchievementViewItemControl Width="60" ShowProgressBar="False" Margin="0,0,16,0"
                                                Style="{StaticResource PreviewThemeControlStyle}"
                                                ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                            <modern:AchievementViewItemControl Width="100" ShowProgressBar="True"
                                                Style="{StaticResource PreviewThemeControlStyle}"
                                                ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                        -->

                        <!-- ================== PIE CHART PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_PieChartPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementPieChartControl
                                            Width="260"
                                            Height="260"
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- ================== BAR CHART PREVIEW SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_BarChartPreview}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>
                                <Border Style="{StaticResource SectionAccentRuleStyle}"/>
                                <Border Style="{StaticResource SettingGroupCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="PREVIEW"
                                                   Style="{StaticResource ContextLabelStyle}"/>
                                        <modern:AchievementBarChartControl Width="280" Height="120"
                                            Style="{StaticResource PreviewThemeControlStyle}"
                                            ThemeDataOverride="{Binding PreviewThemeData, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: STEAM SETTINGS ================== -->
            <TabItem x:Name="SteamTab" Header="{DynamicResource LOCPlayAch_Provider_Steam}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.SteamEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.SteamEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- ================== STEAM CONNECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.SteamEnabled}">
                            <StackPanel>
                                <!-- Steam Authentication (unified box for API key + web auth) -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_SteamAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <!-- Unified Auth Card (green if BOTH api key + web auth, red otherwise) -->
                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SteamFullyConfigured, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <!-- Web Auth Section -->
                                            <StackPanel Margin="0,0,0,14">
                                                <!-- Status Row -->
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Status Icon -->
                                                    <TextBlock Grid.Column="0"
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="{DynamicResource FontSize}"
                                                               Margin="0,0,12,0"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.RenderTransform>
                                                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                        </TextBlock.RenderTransform>
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding SteamFullyConfigured, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                 Value="True">
                                                                        <Setter Property="Text" Value="&#xE73E;"/>
                                                                        <Setter Property="Foreground" Value="#06FFA5"/>
                                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding SteamFullyConfigured, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                 Value="False">
                                                                        <Setter Property="Text" Value="&#xE711;"/>
                                                                        <Setter Property="Foreground" Value="#F72585"/>
                                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <!-- Status Text -->
                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding SteamAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               TextWrapping="Wrap"
                                                               VerticalAlignment="Center"
                                                               FontSize="{DynamicResource FontSize}"
                                                               FontWeight="SemiBold"/>
                                                </Grid>

                                                <!-- Buttons Row (left-aligned) -->
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="{DynamicResource LOCPlayAch_Settings_CheckSteamAuth}"
                                                            Style="{StaticResource SteamAuthActionButtonStyle}"
                                                            Click="SteamAuth_Check_Click"
                                                            Padding="14,6"
                                                            Margin="0,0,8,0"/>
                                                    <Button Content="{DynamicResource LOCPlayAch_Settings_ReauthSteam}"
                                                            Style="{StaticResource SteamAuthActionButtonStyle}"
                                                            Click="SteamAuth_Authenticate_Click"
                                                            Padding="14,6"
                                                            Margin="0,0,8,0"/>
                                                        <Button Content="{DynamicResource LOCPlayAch_Button_Clear}"
                                                            ToolTip="{DynamicResource LOCPlayAch_Button_Clear}"
                                                            Style="{StaticResource SteamAuthActionButtonStyle}"
                                                            Click="SteamAuth_Clear_Click"
                                                            Padding="14,6"/>
                                                </StackPanel>

                                                <!-- Progress bar under buttons -->
                                                <ProgressBar Height="3"
                                                             Margin="0,8,0,0"
                                                             IsIndeterminate="True"
                                                             Foreground="#06FFA5">
                                                    <ProgressBar.Style>
                                                        <Style TargetType="ProgressBar">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding SteamAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ProgressBar.Style>
                                                </ProgressBar>
                                            </StackPanel>

                                            <!-- Separator -->
                                            <Border Height="1"
                                                    Background="{DynamicResource ControlBorderBrush}"
                                                    Opacity="0.5"
                                                    Margin="0,0,0,14"/>

                                            <!-- API Key Section -->
                                            <StackPanel>
                                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_SteamApiKey}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           Margin="0,0,0,6"/>

                                                <TextBox Text="{Binding Persisted.SteamApiKey, UpdateSourceTrigger=PropertyChanged}"
                                                         KeyDown="SteamApiKey_KeyDown"
                                                         LostFocus="SteamApiKey_LostFocus"
                                                         Margin="0,0,0,6"/>

                                                <TextBlock Style="{StaticResource MutedTextStyle}">
                                                    <Run Text="{DynamicResource LOCPlayAch_Settings_SteamApiKeyHint}" />
                                                    <Run Text=" " />
                                                    <Hyperlink NavigateUri="https://steamcommunity.com/dev/apikey"
                                                               RequestNavigate="Hyperlink_RequestNavigate">
                                                        <Run Text="steamcommunity.com/dev/apikey" />
                                                    </Hyperlink>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <!-- Steam User ID (at bottom, read-only tint) -->
                                <Border Background="#10000000"
                                        CornerRadius="6"
                                        Padding="12"
                                        Margin="0,0,0,0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="168"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0"
                                                   Text="{DynamicResource LOCPlayAch_Label_SteamUserId}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   VerticalAlignment="Center"
                                                   Opacity="0.7"/>

                                            <TextBox Grid.Row="0" Grid.Column="1"
                                                     Text="{Binding Persisted.SteamUserId, UpdateSourceTrigger=PropertyChanged}"
                                                 IsReadOnly="True"
                                                 VerticalAlignment="Center"
                                                 BorderThickness="0"
                                                 Background="Transparent"
                                                 Foreground="{DynamicResource TextBrush}"
                                                 Opacity="0.6"/>

                                        <TextBlock Grid.Row="1" Grid.Column="1"
                                                   Margin="0,6,0,0"
                                                   Style="{StaticResource MutedTextStyle}"
                                                   Opacity="0.5">
                                            <Run Text="{DynamicResource LOCPlayAch_Label_SteamUserId_Help}" />
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: RETROACHIEVEMENTS SETTINGS ================== -->
            <TabItem x:Name="RetroAchievementsTab" Header="{DynamicResource LOCPlayAch_Provider_RetroAchievements}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.RetroAchievementsEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.RetroAchievementsEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.RetroAchievementsEnabled}">
                            <StackPanel>
                                <!-- RetroAchievements Authentication (unified box) -->
                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_RaAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"
                                               TextWrapping="Wrap"/>

                                    <!-- Unified Auth Card (green if BOTH username + API key, red otherwise) -->
                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RaFullyConfigured, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Status Row -->
                                            <Grid Grid.Row="0" Margin="0,0,0,14">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Status Icon -->
                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding RaFullyConfigured, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding RaFullyConfigured, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                </TextBlock>

                                                <!-- Status Text -->
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding RaAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <!-- Separator -->
                                            <Border Grid.Row="0" Height="1"
                                                    Background="{DynamicResource ControlBorderBrush}"
                                                    Opacity="0.5"
                                                    Margin="36,0,0,14"/>

                                            <!-- Credentials Section -->
                                            <StackPanel Grid.Row="1">
                                                <!-- Username -->
                                                <StackPanel Margin="0,0,0,12">
                                                    <TextBlock Text="{DynamicResource LOCPlayAch_Label_Username}"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="{DynamicResource FontSizeSmall}"
                                                               Opacity="0.8"
                                                               Margin="0,0,0,4"/>
                                                    <TextBox Text="{Binding Persisted.RaUsername, UpdateSourceTrigger=PropertyChanged}"
                                                             KeyDown="RaUsername_KeyDown"
                                                             LostFocus="RaUsername_LostFocus"
                                                             Margin="0,0,0,4"/>
                                                    <TextBlock Style="{StaticResource MutedTextStyle}">
                                                        <Run Text="{DynamicResource LOCPlayAch_Label_Username_Help}" />
                                                    </TextBlock>
                                                </StackPanel>

                                                <!-- API Key -->
                                                <StackPanel>
                                                    <TextBlock Text="{DynamicResource LOCPlayAch_Label_WebApiKey}"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="{DynamicResource FontSizeSmall}"
                                                               Opacity="0.8"
                                                               Margin="0,0,0,4"/>
                                                    <TextBox Text="{Binding Persisted.RaWebApiKey, UpdateSourceTrigger=PropertyChanged}"
                                                             KeyDown="RaApiKey_KeyDown"
                                                             LostFocus="RaApiKey_LostFocus"
                                                             Margin="0,0,0,4"/>
                                                    <TextBlock Style="{StaticResource MutedTextStyle}">
                                                        <Run Text="{DynamicResource LOCPlayAch_Label_WebApiKey_Help}" />
                                                        <Run Text=" " />
                                                        <Hyperlink NavigateUri="https://retroachievements.org/controlpanel.php"
                                                                   RequestNavigate="Hyperlink_RequestNavigate">
                                                            <Run Text="retroachievements.org/controlpanel.php" />
                                                        </Hyperlink>
                                                    </TextBlock>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Grid>

                                <!-- RetroAchievements Rarity Stats -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_RaRarityStats}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Center"/>

                                        <ComboBox Grid.Row="0" Grid.Column="1"
                                                  SelectedValue="{Binding Persisted.RaRarityStats}"
                                              SelectedValuePath="Tag"
                                              VerticalAlignment="Center"
                                              MinWidth="160"
                                              HorizontalAlignment="Left">
                                        <ComboBoxItem Content="{DynamicResource LOCPlayAch_Settings_RaRarityStats_Casual}" Tag="casual"/>
                                        <ComboBoxItem Content="{DynamicResource LOCPlayAch_Settings_RaRarityStats_Hardcore}" Tag="hardcore"/>
                                        <ComboBoxItem Content="{DynamicResource LOCPlayAch_Settings_RaRarityStats_Combined}" Tag="combined"/>
                                    </ComboBox>

                                    <TextBlock Grid.Row="1" Grid.Column="1"
                                               Text="{DynamicResource LOCPlayAch_Settings_RaRarityStats_Description}"
                                               Margin="0,6,0,0"
                                               Style="{StaticResource MutedTextStyle}"/>
                                </Grid>

                                <!-- RetroAchievements Points Display Mode -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_RaPointsMode}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Center"/>

                                    <ComboBox Grid.Row="0" Grid.Column="1"
                                              SelectedValue="{Binding Persisted.RaPointsMode}"
                                              SelectedValuePath="Tag"
                                              VerticalAlignment="Center"
                                              MinWidth="160"
                                              HorizontalAlignment="Left">
                                        <ComboBoxItem Content="{DynamicResource LOCPlayAch_Settings_RaPointsMode_Points}" Tag="points"/>
                                        <ComboBoxItem Content="{DynamicResource LOCPlayAch_Settings_RaPointsMode_Scaled}" Tag="scaled"/>
                                    </ComboBox>

                                    <TextBlock Grid.Row="1" Grid.Column="1"
                                               Text="{DynamicResource LOCPlayAch_Settings_RaPointsMode_Description}"
                                               Margin="0,6,0,0"
                                               Style="{StaticResource MutedTextStyle}"/>
                                </Grid>

                                 <!-- Refreshing options -->
                                 <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ScanningHashing}"
                                            Style="{StaticResource SectionHeaderStyle}"
                                            Margin="0,8,0,12"/>

                                <StackPanel>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnableArchiveScanning}"
                                                  IsChecked="{Binding Persisted.EnableArchiveScanning}"
                                              Margin="0,0,0,10"/>
                                    <CheckBox Content="{DynamicResource LOCPlayAch_Settings_EnableDiscHashing}"
                                                  IsChecked="{Binding Persisted.EnableDiscHashing}"
                                              Margin="0,0,0,10"/>
                                </StackPanel>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_HashIndexMaxAgeDays}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Center"/>

                                        <TextBox Grid.Column="1"
                                                 Text="{Binding Persisted.HashIndexMaxAgeDays, UpdateSourceTrigger=PropertyChanged}"
                                             Width="60"
                                             TextAlignment="Right"
                                             VerticalAlignment="Center"/>
                                </Grid>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_ForceHashIndexRebuild}"
                                               Style="{StaticResource MutedTextStyle}"
                                               VerticalAlignment="Center"/>

                                    <Button Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_RebuildNow}"
                                            Click="ForceRebuildHashIndex_Click"
                                            Padding="14,6"
                                            Margin="16,0,0,0"
                                            MinWidth="90"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: GOG SETTINGS ================== -->
            <TabItem x:Name="GogTab" Header="{DynamicResource LOCPlayAch_Provider_GOG}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.GogEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.GogEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.GogEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_GogConnection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           Text="{DynamicResource LOCPlayAch_Settings_Gog_OwnAuthHelp}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_GogAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                                VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding GogAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding GogAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding GogAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding GogAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <!-- Login/Logout buttons -->
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Login}"
                                                        Style="{StaticResource GogAuthActionButtonStyle}"
                                                        Click="GogAuth_Login_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Clear}"
                                                        Style="{StaticResource GogAuthActionButtonStyle}"
                                                        Click="GogAuth_Clear_Click"
                                                        Padding="14,6"/>
                                            </StackPanel>

                                            <ProgressBar Height="3"
                                                         Margin="0,8,0,0"
                                                         IsIndeterminate="True"
                                                         Foreground="#06FFA5">
                                                <ProgressBar.Style>
                                                    <Style TargetType="ProgressBar">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding GogAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ProgressBar.Style>
                                            </ProgressBar>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: EPIC SETTINGS ================== -->
            <TabItem x:Name="EpicTab" Header="{DynamicResource LOCPlayAch_Provider_Epic}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.EpicEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.EpicEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.EpicEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_EpicConnection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           Text="{DynamicResource LOCPlayAch_Settings_Epic_OwnAuthHelp}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_EpicAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EpicAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding EpicAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding EpicAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding EpicAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <!-- Login/Logout buttons -->
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Login}"
                                                        Style="{StaticResource EpicAuthActionButtonStyle}"
                                                        Click="EpicAuth_Login_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Settings_Epic_LoginAlternative}"
                                                        Style="{StaticResource EpicAuthActionButtonStyle}"
                                                        Click="EpicAuth_LoginAlternative_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Clear}"
                                                        Style="{StaticResource EpicAuthActionButtonStyle}"
                                                        Click="EpicAuth_Clear_Click"
                                                        Padding="14,6"/>
                                            </StackPanel>

                                            <ProgressBar Height="3"
                                                         Margin="0,8,0,0"
                                                         IsIndeterminate="True"
                                                         Foreground="#06FFA5">
                                                <ProgressBar.Style>
                                                    <Style TargetType="ProgressBar">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding EpicAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ProgressBar.Style>
                                            </ProgressBar>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: PLAYSTATION SETTINGS ================== -->
            <TabItem x:Name="PsnTab" Header="{DynamicResource LOCPlayAch_Provider_PSN}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.PsnEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.PsnEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.PsnEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_PsnConnection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <!-- NPSSO Instructions -->
                                <StackPanel Margin="0,0,0,16">
                                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4">
                                        <Run Text="1. "/><Hyperlink NavigateUri="https://www.playstation.com" RequestNavigate="Hyperlink_RequestNavigate" TextDecorations="Underline" FontWeight="SemiBold"><TextBlock Foreground="#4CC9F0" Text="{DynamicResource LOCPlayAch_Settings_Psn_Step1}"/></Hyperlink><Run Text=" "/><Run Text="{DynamicResource LOCPlayAch_Settings_Psn_Step1Note}"/>
                                    </TextBlock>
                                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4">
                                        <Run Text="2. "/><Hyperlink NavigateUri="https://ca.account.sony.com/api/v1/ssocookie" RequestNavigate="Hyperlink_RequestNavigate" TextDecorations="Underline" FontWeight="SemiBold"><TextBlock Foreground="#4CC9F0" Text="{DynamicResource LOCPlayAch_Settings_Psn_Step2}"/></Hyperlink><Run Text=" "/><Run Text="{DynamicResource LOCPlayAch_Settings_Psn_Step2Note}"/>
                                    </TextBlock>
                                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}">
                                        <Run Text="3. "/><Run Text="{DynamicResource LOCPlayAch_Settings_Psn_Step3}"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- NPSSO Cookie Input -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_PsnNpsso}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <TextBox Grid.Column="1"
                                             Text="{Binding Persisted.PsnNpsso, UpdateSourceTrigger=PropertyChanged}"
                                             x:Name="PsnNpssoTextBox"
                                             Style="{StaticResource {x:Type TextBox}}"
                                             Padding="8,6"
                                             VerticalAlignment="Top"
                                             MaxHeight="60"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="False"
                                             VerticalScrollBarVisibility="Auto"
                                             KeyDown="PsnNpsso_KeyDown"
                                             LostFocus="PsnNpsso_LostFocus"/>
                                </Grid>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           Text="{DynamicResource LOCPlayAch_Settings_PsnNpssoHint}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_PsnAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PsnAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PsnAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding PsnAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding PsnAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="{DynamicResource LOCPlayAch_Settings_Psn_Check}"
                                                        Style="{StaticResource PsnAuthActionButtonStyle}"
                                                        Click="PsnAuth_Check_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Settings_Psn_LoginLegacy}"
                                                        Style="{StaticResource PsnAuthActionButtonStyle}"
                                                        Click="PsnAuth_Login_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Clear}"
                                                        Style="{StaticResource PsnAuthActionButtonStyle}"
                                                        Click="PsnAuth_Clear_Click"
                                                        Padding="14,6"/>
                                            </StackPanel>

                                            <ProgressBar Height="3"
                                                         Margin="0,8,0,0"
                                                         IsIndeterminate="True"
                                                         Foreground="#06FFA5">
                                                <ProgressBar.Style>
                                                    <Style TargetType="ProgressBar">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding PsnAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ProgressBar.Style>
                                            </ProgressBar>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: XBOX SETTINGS ================== -->
            <TabItem x:Name="XboxTab" Header="{DynamicResource LOCPlayAch_Provider_Xbox}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.XboxEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.XboxEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.XboxEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_XboxConnection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           Text="{DynamicResource LOCPlayAch_Settings_Xbox_Help}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_XboxAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding XboxAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding XboxAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding XboxAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding XboxAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Login}"
                                                        Style="{StaticResource XboxAuthActionButtonStyle}"
                                                        Click="XboxAuth_Login_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Clear}"
                                                        Style="{StaticResource XboxAuthActionButtonStyle}"
                                                        Click="XboxAuth_Clear_Click"
                                                        Padding="14,6"/>
                                            </StackPanel>

                                            <ProgressBar Height="3"
                                                         Margin="0,8,0,0"
                                                         IsIndeterminate="True"
                                                         Foreground="#06FFA5">
                                                <ProgressBar.Style>
                                                    <Style TargetType="ProgressBar">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding XboxAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ProgressBar.Style>
                                            </ProgressBar>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Low-Res Icons Toggle -->
                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_XboxLowResIcons}"
                                  IsChecked="{Binding Persisted.XboxLowResIcons}"
                                  IsEnabled="{Binding Persisted.XboxEnabled}"
                                  Margin="0,0,0,4"/>
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_XboxLowResIcons_Help}"
                                   Margin="24,0,0,16"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.XboxEnabled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: EXOPHASE PROVIDER ================== -->
            <TabItem x:Name="ExophaseTab" Header="{DynamicResource LOCPlayAch_Provider_Exophase}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <StackPanel>
                                <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                          IsChecked="{Binding Persisted.ExophaseEnabled}"
                                          FontWeight="SemiBold"
                                          FontSize="{DynamicResource FontSize}"/>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Exophase_EnableProvider_Help}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="24,4,0,0"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.ExophaseEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Authentication Section -->
                        <Border Style="{StaticResource SectionBorderStyle}"
                                Margin="0,0,0,16"
                                IsEnabled="{Binding Persisted.ExophaseEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ExophaseConnection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           TextWrapping="Wrap">
                                    <Run Text="{DynamicResource LOCPlayAch_Settings_ExophaseAuth_Description}"/>
                                    <LineBreak/>
                                    <Run Text="{DynamicResource LOCPlayAch_Settings_Exophase_Help}"/>
                                </TextBlock>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Label_ExophaseAuth}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ExophaseAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ExophaseAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ExophaseAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding ExophaseAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Login}"
                                                        Style="{StaticResource ExophaseAuthActionButtonStyle}"
                                                        Click="ExophaseAuth_Login_Click"
                                                        Padding="14,6"
                                                        Margin="0,0,8,0"/>
                                                <Button Content="{DynamicResource LOCPlayAch_Button_Clear}"
                                                        Style="{StaticResource ExophaseAuthActionButtonStyle}"
                                                        Click="ExophaseAuth_Clear_Click"
                                                        Padding="14,6"/>
                                            </StackPanel>

                                            <ProgressBar Height="3"
                                                         Margin="0,8,0,0"
                                                         IsIndeterminate="True"
                                                         Foreground="#06FFA5">
                                                <ProgressBar.Style>
                                                    <Style TargetType="ProgressBar">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ExophaseAuthBusy, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ProgressBar.Style>
                                            </ProgressBar>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Managed Platforms -->
                        <Border Style="{StaticResource SectionBorderStyle}"
                                Margin="0,0,0,16"
                                IsEnabled="{Binding Persisted.ExophaseEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Exophase_ManagedPlatforms}"
                                           Style="{StaticResource SectionHeaderStyle}"/>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Exophase_ManagedPlatforms_Help}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,12"/>

                                <Border Background="#10000000"
                                        CornerRadius="6"
                                        Padding="12"
                                        Margin="0,4,0,0">
                                    <WrapPanel Orientation="Horizontal"
                                               ItemWidth="150"
                                               Margin="0,2,0,0">
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_Steam}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="steam"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_PSN}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="psn"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_Xbox}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="xbox"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_GOG}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="gog"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_Epic}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="epic"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_EA}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="ea"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_Blizzard}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="blizzard"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_Nintendo}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="nintendo"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                        <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Exophase_Platform_Retro}"
                                                  Loaded="ExophasePlatform_CheckboxLoaded"
                                                  Click="ExophasePlatform_CheckChanged"
                                                  Tag="retro"
                                                  Margin="0,0,12,10"
                                                  FontSize="{DynamicResource FontSize}"/>
                                    </WrapPanel>
                                </Border>

                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Exophase_Note}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,12,0,0"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: SHADPS4 ================== -->
            <TabItem x:Name="ShadPS4Tab" Header="{DynamicResource LOCPlayAch_Provider_ShadPS4}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.ShadPS4Enabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.ShadPS4Enabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.ShadPS4Enabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ShadPS4Connection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           Text="{DynamicResource LOCPlayAch_Settings_ShadPS4_Description}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_ShadPS4GameDataPath}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ShadPS4Authenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ShadPS4Authenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding ShadPS4Authenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding ShadPS4AuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ShadPS4GameDataPathHint}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       Margin="0,0,0,12"
                                                       TextWrapping="Wrap"/>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBox Grid.Column="0"
                                                         Text="{Binding Persisted.ShadPS4GameDataPath, UpdateSourceTrigger=PropertyChanged}"
                                                         KeyDown="ShadPS4GameDataPath_KeyDown"
                                                         LostFocus="ShadPS4GameDataPath_LostFocus"
                                                         VerticalAlignment="Center"
                                                         Padding="8,6"
                                                         Margin="0,0,8,0"/>

                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource LOCPlayAch_Settings_ShadPS4_Browse}"
                                                        Click="ShadPS4_Browse_Click"
                                                        Padding="14,6"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: RPCS3 ================== -->
            <TabItem x:Name="Rpcs3Tab" Header="{DynamicResource LOCPlayAch_Provider_RPCS3}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.Rpcs3Enabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                                   Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.Rpcs3Enabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.Rpcs3Enabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Rpcs3Connection}"
                                           Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"
                                           Text="{DynamicResource LOCPlayAch_Settings_Rpcs3_Description}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{DynamicResource LOCPlayAch_Settings_Rpcs3ExecutablePath}"
                                               Foreground="{DynamicResource TextBrush}"
                                               VerticalAlignment="Top"
                                               Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Rpcs3Authenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="{DynamicResource FontSize}"
                                                           Margin="0,0,12,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Rpcs3Authenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Rpcs3Authenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Rpcs3AuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center"
                                                           FontSize="{DynamicResource FontSize}"
                                                           FontWeight="SemiBold"/>
                                            </Grid>

                                            <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Rpcs3ExecutablePathHint}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       Margin="0,0,0,12"
                                                       TextWrapping="Wrap"/>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBox Grid.Column="0"
                                                         Text="{Binding Persisted.Rpcs3ExecutablePath, UpdateSourceTrigger=PropertyChanged}"
                                                         KeyDown="Rpcs3ExecutablePath_KeyDown"
                                                         LostFocus="Rpcs3ExecutablePath_LostFocus"
                                                         VerticalAlignment="Center"
                                                         Padding="8,6"
                                                         Margin="0,0,8,0"/>

                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource LOCPlayAch_Settings_Rpcs3_Browse}"
                                                        Click="Rpcs3_Browse_Click"
                                                        Padding="14,6"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: Xenia ================== -->
            <TabItem x:Name="XeniaTab" Header="{DynamicResource LOCPlayAch_Provider_Xenia}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                   HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                           IsChecked="{Binding Persisted.XeniaEnabled}"
                           FontWeight="SemiBold"
                           FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <!-- Provider Disabled Message -->
                        <TextBlock Text="{DynamicResource LOCPlayAch_Settings_ProviderDisabledMessage}"
                        Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Persisted.XeniaEnabled}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Style="{StaticResource SectionBorderStyle}" IsEnabled="{Binding Persisted.XeniaEnabled}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_XeniaConnection}"
                                Style="{StaticResource SectionHeaderStyle}"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                Margin="0,0,0,16"
                                Text="{DynamicResource LOCPlayAch_Settings_Xenia_Description}"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                    Text="{DynamicResource LOCPlayAch_Settings_XeniaAccountPath}"
                                    Foreground="{DynamicResource TextBrush}"
                                    VerticalAlignment="Top"
                                    Margin="0,8,0,0"/>

                                    <Border Grid.Column="1"
                                 Background="{DynamicResource PanelBackgroundBrush}"
                                 CornerRadius="8"
                                 Padding="16"
                                 BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#F72585"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding XeniaAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      Value="True">
                                                        <Setter Property="BorderBrush" Value="#06FFA5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="{DynamicResource FontSize}"
                                                Margin="0,0,12,0"
                                                VerticalAlignment="Center">
                                                    <TextBlock.RenderTransform>
                                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                                    </TextBlock.RenderTransform>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding XeniaAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  Value="True">
                                                                    <Setter Property="Text" Value="&#xE73E;"/>
                                                                    <Setter Property="Foreground" Value="#06FFA5"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding XeniaAuthenticated, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  Value="False">
                                                                    <Setter Property="Text" Value="&#xE711;"/>
                                                                    <Setter Property="Foreground" Value="#F72585"/>
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBlock Grid.Column="1"
                                                Text="{Binding XeniaAuthStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Foreground="{DynamicResource TextBrush}"
                                                TextWrapping="Wrap"
                                                VerticalAlignment="Center"
                                                FontSize="{DynamicResource FontSize}"
                                                FontWeight="SemiBold"/>
                                            </Grid>

                                            <TextBlock Text="{DynamicResource LOCPlayAch_Settings_XeniaAccountPathHint}"
                                            Style="{StaticResource MutedTextStyle}"
                                            Margin="0,0,0,12"
                                            TextWrapping="Wrap"/>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBox Grid.Column="0"
                                              Text="{Binding Persisted.XeniaAccountPath, UpdateSourceTrigger=PropertyChanged}"
                                              KeyDown="XeniaAccountPath_KeyDown"
                                              LostFocus="XeniaAccountPath_LostFocus"
                                              VerticalAlignment="Center"
                                              Padding="8,6"
                                              Margin="0,0,8,0"/>

                                                <Button Grid.Column="1"
                                             Content="{DynamicResource LOCPlayAch_Settings_Xenia_Browse}"
                                             Click="Xenia_Browse_Click"
                                             Padding="14,6"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>


            <!-- ================== TAB: MANUAL ================== -->
            <TabItem x:Name="ManualTab" Header="{DynamicResource LOCPlayAch_Provider_Manual}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- Provider Enable Toggle -->
                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <CheckBox Content="{DynamicResource LOCPlayAch_Settings_ProviderEnabled}"
                                      IsChecked="{Binding Persisted.ManualEnabled}"
                                      FontWeight="SemiBold"
                                      FontSize="{DynamicResource FontSize}"/>
                        </Border>

                        <Border Style="{StaticResource SectionBorderStyle}" Margin="0,0,0,12">
                            <StackPanel>
                                <CheckBox Content="{DynamicResource LOCPlayAch_Settings_Manual_Override_Enable}"
                                          IsChecked="{Binding Persisted.ManualTrackingOverrideEnabled}"
                                          FontWeight="SemiBold"
                                          FontSize="{DynamicResource FontSize}"
                                          Margin="0,0,0,6"/>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Manual_Override_Description}"
                                           Style="{StaticResource MutedTextStyle}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Manual_Legacy_Header}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>

                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Manual_Legacy_Description}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16"/>

                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Manual_Legacy_PathLabel}"
                                           Foreground="{DynamicResource TextBrush}"
                                           Margin="0,0,0,6"/>

                                <TextBlock Text="{DynamicResource LOCPlayAch_Settings_Manual_Legacy_PathHint}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,10"/>

                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0"
                                             Text="{Binding Persisted.LegacyManualImportPath, UpdateSourceTrigger=PropertyChanged}"
                                             VerticalAlignment="Center"
                                             Padding="8,6"
                                             Margin="0,0,8,0"/>

                                    <Button Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_Settings_Manual_Legacy_Browse}"
                                            Click="ManualLegacyBrowse_Click"
                                            Padding="14,6"/>
                                </Grid>

                                <Button Content="{DynamicResource LOCPlayAch_Settings_Manual_Legacy_ImportButton}"
                                        Click="ManualLegacyImport_Click"
                                        Padding="14,6"
                                        Margin="0,0,0,10"
                                        HorizontalAlignment="Left"
                                        MinWidth="170">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LegacyManualImportBusy, ElementName=SettingsControlRoot}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <TextBlock Text="{Binding LegacyManualImportStatus, ElementName=SettingsControlRoot}"
                                           Style="{StaticResource MutedTextStyle}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ================== TAB: THEME MIGRATION ================== -->
            <TabItem x:Name="ThemeMigrationTab" Header="{DynamicResource LOCPlayAch_ThemeMigration_Title}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="20">

                        <!-- ================== MIGRATION SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_Title}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16">
                                    <Run Text="{DynamicResource LOCPlayAch_ThemeMigration_Description}"/>
                                    <LineBreak/>
                                    <Run Text="{DynamicResource LOCPlayAch_ThemeMigration_BackupNote}"/>
                                </TextBlock>

                                <!-- Theme Dropdown -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <ComboBox ItemsSource="{Binding AvailableThemes, ElementName=SettingsControlRoot}"
                                              DisplayMemberPath="BestDisplayName"
                                              SelectedValuePath="Path"
                                              SelectedValue="{Binding SelectedThemePath, ElementName=SettingsControlRoot}"
                                              VerticalContentAlignment="Center"
                                              IsEnabled="{Binding HasThemesToMigrate, ElementName=SettingsControlRoot}"/>
                                </Grid>

                                <!-- Migration Buttons -->
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Right"
                                            x:Name="ThemeMigrationPresetButtons"
                                            Margin="0,12,0,0">
                                    <Button Content="{DynamicResource LOCPlayAch_ThemeMigration_MigrationLimited}"
                                            Click="MigrateThemeLimited_Click"
                                            Padding="14,6"
                                            Margin="0,0,8,0"
                                            MinWidth="100"
                                            IsEnabled="{Binding HasThemesToMigrate, ElementName=SettingsControlRoot}"
                                            ToolTipService.ShowOnDisabled="True">
                                        <Button.ToolTip>
                                            <ToolTip>
                                                <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_LimitedTooltip}"
                                                           TextWrapping="Wrap"
                                                           MaxWidth="250"/>
                                            </ToolTip>
                                        </Button.ToolTip>
                                    </Button>
                                    <Button Content="{DynamicResource LOCPlayAch_ThemeMigration_MigrationFull}"
                                            Click="MigrateThemeFull_Click"
                                            Padding="14,6"
                                            MinWidth="100"
                                            IsEnabled="{Binding HasThemesToMigrate, ElementName=SettingsControlRoot}"
                                            ToolTipService.ShowOnDisabled="True">
                                        <Button.ToolTip>
                                            <ToolTip>
                                                <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_FullTooltip}"
                                                           TextWrapping="Wrap"
                                                           MaxWidth="250"/>
                                            </ToolTip>
                                        </Button.ToolTip>
                                    </Button>
                                </StackPanel>

                                <Border Background="#12000000"
                                        BorderBrush="{DynamicResource NormalBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,16,0,0"
                                        Padding="0"
                                        IsEnabled="{Binding HasThemesToMigrate, ElementName=SettingsControlRoot}">
                                    <Expander x:Name="ThemeMigrationCustomExpander"
                                              IsExpanded="False"
                                              Expanded="ThemeMigrationCustomExpander_Expanded"
                                              Collapsed="ThemeMigrationCustomExpander_Collapsed"
                                              Background="Transparent"
                                              BorderThickness="0">
                                        <Expander.Header>
                                            <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_Custom}"
                                                       Foreground="{DynamicResource TextBrush}"
                                                       FontWeight="SemiBold"
                                                       Margin="-4,8,0,8"/>
                                        </Expander.Header>

                                        <StackPanel Margin="12,0,12,12">
                                            <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_CustomDescription}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       HorizontalAlignment="Left"
                                                       Margin="0,0,0,12"
                                                       MaxWidth="520"/>

                                            <Grid Margin="0,0,0,12"
                                                  HorizontalAlignment="Left">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="176"/>
                                                    <ColumnDefinition Width="108"/>
                                                    <ColumnDefinition Width="54"/>
                                                    <ColumnDefinition Width="108"/>
                                                </Grid.ColumnDefinitions>

                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource LOCPlayAch_ThemeMigration_AllLegacy}"
                                                        Click="ThemeMigrationSetAllLegacy_Click"
                                                        Style="{StaticResource MigrationChoiceButtonBaseStyle}"
                                                        Margin="0,0,8,0"/>
                                                <Button Grid.Column="3"
                                                        Content="{DynamicResource LOCPlayAch_ThemeMigration_AllModern}"
                                                        Click="ThemeMigrationSetAllModern_Click"
                                                        Style="{StaticResource MigrationChoiceButtonBaseStyle}"/>
                                            </Grid>

                                            <ItemsControl ItemsSource="{Binding ThemeMigrationCustomOptions, ElementName=SettingsControlRoot}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,0,0,10"
                                                              HorizontalAlignment="Left">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="176"/>
                                                                <ColumnDefinition Width="108"/>
                                                                <ColumnDefinition Width="54"/>
                                                                <ColumnDefinition Width="108"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Text="{Binding DisplayName}"
                                                                       Foreground="{DynamicResource TextBrush}"
                                                                       VerticalAlignment="Center"
                                                                       Margin="0,0,12,0"/>

                                                            <Button Grid.Column="1"
                                                                    Content="{DynamicResource LOCPlayAch_ThemeMigration_ModeLegacy}"
                                                                    Style="{StaticResource MigrationChoiceLegacyButtonStyle}"
                                                                    Click="ThemeMigrationRowSetLegacy_Click"
                                                                    CommandParameter="{Binding}"
                                                                    Margin="0,0,8,0"/>

                                                            <ToggleButton Grid.Column="2"
                                                                          Style="{StaticResource MigrationSwitchToggleStyle}"
                                                                          IsChecked="{Binding IsModern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                                            <Button Grid.Column="3"
                                                                    Content="{DynamicResource LOCPlayAch_ThemeMigration_ModeModern}"
                                                                    Style="{StaticResource MigrationChoiceModernButtonStyle}"
                                                                    Click="ThemeMigrationRowSetModern_Click"
                                                                    CommandParameter="{Binding}"
                                                                    Margin="8,0,0,0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>

                                            <Button Content="{DynamicResource LOCPlayAch_ThemeMigration_MigrationCustom}"
                                                    Click="MigrateThemeCustom_Click"
                                                    Padding="14,6"
                                                    Margin="0,6,0,0"
                                                    MinWidth="100"
                                                    HorizontalAlignment="Right"
                                                    IsEnabled="{Binding HasThemesToMigrate, ElementName=SettingsControlRoot}"/>
                                        </StackPanel>
                                    </Expander>
                                </Border>

                                <!-- No themes message -->
                                <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_NoThemesMessage}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,8,0,0"
                                           Visibility="{Binding ShowNoThemesMessage, ElementName=SettingsControlRoot, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </StackPanel>
                        </Border>

                        <!-- ================== REVERT SECTION ================== -->
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_Revert}"
                                           Style="{StaticResource SectionHeaderStyle}"
                                           Margin="0,0,0,12"/>

                                <TextBlock Style="{StaticResource MutedTextStyle}"
                                           Margin="0,0,0,16">
                                    <Run Text="{DynamicResource LOCPlayAch_ThemeMigration_RevertDescription}"/>
                                </TextBlock>

                                <!-- Revertable Theme Dropdown and Revert Button -->
                                <Grid Style="{StaticResource SettingRowStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding RevertableThemes, ElementName=SettingsControlRoot}"
                                              DisplayMemberPath="BestDisplayName"
                                              SelectedValuePath="Path"
                                              SelectedValue="{Binding SelectedRevertThemePath, ElementName=SettingsControlRoot}"
                                              VerticalContentAlignment="Center"
                                              IsEnabled="{Binding HasRevertableThemes, ElementName=SettingsControlRoot}"/>

                                    <Button Grid.Column="1"
                                            Content="{DynamicResource LOCPlayAch_ThemeMigration_RevertButton}"
                                            Click="RevertTheme_Click"
                                            Padding="14,6"
                                            Margin="16,0,0,0"
                                            MinWidth="100"
                                            IsEnabled="{Binding HasRevertableThemes, ElementName=SettingsControlRoot}"/>
                                </Grid>

                                <!-- No revertable themes message -->
                                <TextBlock Text="{DynamicResource LOCPlayAch_ThemeMigration_NoRevertableThemes}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,8,0,0"
                                           Visibility="{Binding ShowNoRevertableThemesMessage, ElementName=SettingsControlRoot, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>



