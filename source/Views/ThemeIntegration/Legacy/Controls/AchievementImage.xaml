<UserControl x:Class="PlayniteAchievements.Views.ThemeIntegration.Legacy.Controls.AchievementImage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PlayniteAchievements.Views.ThemeIntegration.Legacy.Controls"
             xmlns:helpers="clr-namespace:PlayniteAchievements.Views.Helpers"
             xmlns:controls="clr-namespace:PlayniteAchievements.Views.Controls"
             xmlns:converters="clr-namespace:PlayniteAchievements.Views.Converters"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" d:DesignHeight="48" d:DesignWidth="48"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PlayniteAchievements;component/Resources/CommonResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Height="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Height}"
          Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Width}"
          ClipToBounds="False"
          UseLayoutRounding="True"
          SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <!-- Border for image container -->
        <Border>
            <Grid ClipToBounds="False"
                  UseLayoutRounding="True"
                  SnapsToDevicePixels="True">
                <!-- Rays glow style: a rotating sunburst behind the icon, replacing the soft halo. -->
                <controls:RarityRayBurst Rarity="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Rarity}">
                    <!-- Same icon the layers below draw, chosen the same way, so the track is traced
                         from whichever variant is actually on screen. -->
                    <controls:RarityRayBurst.SubjectUri>
                        <MultiBinding Converter="{StaticResource LockedIconSelectorConverter}">
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IsLocked" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IconCustom" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="Icon" />
                        </MultiBinding>
                    </controls:RarityRayBurst.SubjectUri>
                    <controls:RarityRayBurst.Style>
                        <Style TargetType="controls:RarityRayBurst">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=AnimateRarityGlows}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="IsActive" Value="True"/>
                                </MultiDataTrigger>
                                <!-- Hardcore achievements use a solid border instead of any glow,
                                     unless the user has turned that border off. -->
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsHardcore}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowHardcoreBorder}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Setter Property="IsActive" Value="False"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </controls:RarityRayBurst.Style>
                </controls:RarityRayBurst>
                <Image Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       helpers:AsyncImage.DecodePixel="256"
                       SnapsToDevicePixels="True"
                       IsHitTestVisible="False"
                       CacheMode="BitmapCache"
                       Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Width}"
                       Height="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Height}"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                    <helpers:AsyncImage.Uri>
                        <MultiBinding Converter="{StaticResource LockedIconSelectorConverter}">
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IsLocked" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IconCustom" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="Icon" />
                        </MultiBinding>
                    </helpers:AsyncImage.Uri>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Effect" Value="{x:Null}"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <!-- Multi-bound so the glow follows the per-tier selection and
                                         re-evaluates when that selection changes. -->
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource RarityGlowForTiers}" ConverterParameter="20">
                                                <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="Rarity"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="SoftGlowTiers"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </MultiDataTrigger>
                                <!-- Hardcore achievements use a solid border instead of the soft glow. -->
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsHardcore}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowHardcoreBorder}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Setter Property="Effect" Value="{x:Null}"/>
                                </MultiDataTrigger>
                                <!-- Fade in/out pulse on the cached glow layer when AnimateRarityGlows is on. -->
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=AnimateRarityGlows}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="helpers:RarityGlowPulse.IsActive" Value="True"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>

                <!-- Edge that comes with the rays: the same layer as the halo above at a blur small
                     enough to read as a line. A blur of the artwork, so it hugs the alpha. -->
                <Image Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       helpers:AsyncImage.DecodePixel="256"
                       SnapsToDevicePixels="True"
                       IsHitTestVisible="False"
                       CacheMode="BitmapCache"
                       Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Width}"
                       Height="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Height}"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                    <helpers:AsyncImage.Uri>
                        <MultiBinding Converter="{StaticResource LockedIconSelectorConverter}">
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IsLocked" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IconCustom" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="Icon" />
                        </MultiBinding>
                    </helpers:AsyncImage.Uri>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Effect" Value="{x:Null}"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource RarityGlowForTiers}" ConverterParameter="4">
                                                <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="Rarity"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="RayGlowTiers"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </MultiDataTrigger>
                                <!-- Hardcore keeps its solid border instead. -->
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsHardcore}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowHardcoreBorder}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Setter Property="Effect" Value="{x:Null}"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>

                <Image Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       helpers:AsyncImage.DecodePixel="256"
                       SnapsToDevicePixels="True"
                       Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Width}"
                       Height="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Height}"
                       x:Name="PART_Image"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Loaded="Image_Loaded" ImageFailed="Image_ImageFailed">
                    <helpers:AsyncImage.Uri>
                        <MultiBinding Converter="{StaticResource LockedIconSelectorConverter}">
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IsLocked" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="IconCustom" />
                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="Icon" />
                        </MultiBinding>
                    </helpers:AsyncImage.Uri>
                </Image>
            </Grid>
        </Border>

        <!-- Solid rarity border for Hardcore achievements (replaces the glow).
             Negative margin draws the border just OUTSIDE the icon so it never shrinks it. -->
        <Border Margin="-3"
                BorderThickness="3"
                Background="Transparent"
                IsHitTestVisible="False"
                SnapsToDevicePixels="True"
                BorderBrush="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Rarity, Converter={StaticResource RarityToShineBrush}}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=EnableRaretyIndicator}" Value="True"/>
                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsLocked}" Value="False"/>
                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowRarityGlow}" Value="True"/>
                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsHardcore}" Value="True"/>
                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowHardcoreBorder}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <!-- Custom icon text overlay (only shown when using custom locked icon) -->
        <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IconText, FallbackValue='🔒'}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" Name="PART_IconText" FontWeight="Bold"
                   Foreground="Black"
                   Visibility="Collapsed">
            <TextBlock.Effect>
                <DropShadowEffect Color="White" Opacity="{StaticResource PlayAch.Opacity.Hint}" BlurRadius="3" ShadowDepth="0" />
            </TextBlock.Effect>
        </TextBlock>

        <!-- Progress bar overlay (always visible per compatibility request). -->
        <Grid x:Name="PART_RarityOverlay"
              VerticalAlignment="Bottom"
              Visibility="Collapsed">
            <ProgressBar Height="12" Name="PART_ProgressBar" Maximum="100" />
            <Label Name="PART_Label"
                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
        </Grid>
    </Grid>
</UserControl>
