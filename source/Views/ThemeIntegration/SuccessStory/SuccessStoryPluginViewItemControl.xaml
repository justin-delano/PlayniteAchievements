<local:ThemeControlBase x:Class="PlayniteAchievements.Views.ThemeIntegration.SuccessStory.SuccessStoryPluginViewItemControl"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:controls="clr-namespace:Playnite.SDK.Controls;assembly=Playnite.SDK"
                   xmlns:converters="clr-namespace:PlayniteAchievements.Views.Converters"
                   xmlns:local="clr-namespace:PlayniteAchievements.Views.ThemeIntegration.Base"
                   mc:Ignorable="d"
                   d:DesignHeight="40" d:DesignWidth="200">
    <!--
        SuccessStory-compatible view item control.
        Matches the original SuccessStory plugin styling.
        Displays achievement summary in game details view.
    -->
    <local:ThemeControlBase.Resources>
        <converters:BooleanConverter x:Key="BooleanToVisibilityConverter" Mode="ToVisibility" />
        <converters:BooleanConverter x:Key="InverseBoolToVisConverter" Mode="InvertedToVisibility" />
    </local:ThemeControlBase.Resources>

    <Grid Name="PART_GridContener">
        <!-- Simple label format (when IntegrationViewItemWithProgressBar=False) -->
        <TextBlock VerticalAlignment="Center"
                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=PluginUserControl}, Path=Foreground}"
                   FontSize="{Binding RelativeSource={RelativeSource AncestorType=PluginUserControl}, Path=FontSize}"
                   Visibility="{Binding RelativeSource={RelativeSource AncestorType=PluginUserControl}, Path=IntegrationViewItemWithProgressBar, Converter={StaticResource InverseBoolToVisConverter}}">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}/{1}">
                    <Binding Path="UnlockedCount" FallbackValue="0" />
                    <Binding Path="AchievementCount" FallbackValue="0" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>

        <!-- Progress bar mode (when IntegrationViewItemWithProgressBar=True) -->
        <Grid Visibility="{Binding RelativeSource={RelativeSource AncestorType=PluginUserControl}, Path=IntegrationViewItemWithProgressBar, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="-" />

            <ProgressBar Grid.Column="1" Value="{Binding UnlockedCount, Mode=OneWay, FallbackValue=20}"
                         Maximum="{Binding AchievementCount, Mode=OneWay}"
                         MinWidth="30"
                         Foreground="{Binding RelativeSource={RelativeSource AncestorType=PluginUserControl}, Path=Foreground}"
                         Height="16" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</local:ThemeControlBase>
