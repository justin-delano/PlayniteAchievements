<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <!-- The plugin's out-of-process helper. Its first argument selects a role; the "sound" role
       renders unlock sounds so the recorder can exclude this process from clip captures, and
       further roles can be added without another exe. Toolbox strips Playnite-provided assemblies
       from the .pext (Newtonsoft.Json, System.ValueTuple, ...), which the plugin resolves from
       Playnite's process but a standalone exe cannot, so this project references only the
       framework and NAudio and uses no C# tuple syntax. The plugin csproj copies the built exe into
       its own output via a ProjectReference with ReferenceOutputAssembly=false. -->
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{7A2E4C51-9B3D-4F6E-8C1A-2D5F7E9B0C43}</ProjectGuid>
    <OutputType>WinExe</OutputType>
    <RootNamespace>PlayniteAchievements.Helper</RootNamespace>
    <AssemblyName>PlayniteAchievementsHelper</AssemblyName>
    <TargetFrameworkVersion>v4.6.2</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <Deterministic>true</Deterministic>
    <Prefer32Bit>false</Prefer32Bit>
    <LangVersion>preview</LangVersion>
    <AutoGenerateBindingRedirects>false</AutoGenerateBindingRedirects>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="NAudio">
      <HintPath>..\packages\NAudio.1.10.0\lib\net35\NAudio.dll</HintPath>
      <Private>True</Private>
    </Reference>
    <Reference Include="System" />
    <Reference Include="System.Core" />
  </ItemGroup>
  <ItemGroup>
    <Compile Include="Program.cs" />
    <Compile Include="SoundRole.cs" />
    <Compile Include="SoundEngine.cs" />
    <!-- Single source of truth for the line protocol, compiled into both the plugin and this exe. -->
    <Compile Include="..\Services\Sound\SoundHostProtocol.cs">
      <Link>SoundHostProtocol.cs</Link>
    </Compile>
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
</Project>
