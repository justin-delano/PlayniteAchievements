namespace PlayniteAchievements.Models.Settings
{
    /// <summary>
    /// Controls how playtime durations are rendered. Display only; sorting and stored values are unaffected.
    /// </summary>
    public enum PlaytimeDisplayMode
    {
        HoursAndMinutes,
        HoursOnly
    }
}
