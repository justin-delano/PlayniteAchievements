<UserControl x:Class="PlayniteAchievements.Providers.Local.LocalImportProgressControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="240"
             d:DesignWidth="420">

    <UserControl.Resources>
        <Style x:Key="ProgressDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource ControlBackgroundBrush}"
                BorderBrush="{DynamicResource NormalBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,12">
            <TextBlock Text="{Binding DialogTitle}"
                       Style="{DynamicResource BaseTextBlockStyle}"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextBrush}"/>
        </Border>

        <Border Grid.Row="1"
                Background="{DynamicResource ControlBackgroundBrush}"
                BorderBrush="{DynamicResource NormalBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,14">
            <StackPanel>
                <TextBlock Text="{Binding ProgressMessage}"
                           Style="{DynamicResource BaseTextBlockStyle}"
                           Foreground="{DynamicResource TextBrush}"
                           Opacity="0.85"
                           TextWrapping="Wrap"
                           MinHeight="42"
                           Margin="0,0,0,12"/>

                <Grid Margin="0,0,0,10">
                    <ProgressBar Value="{Binding ProgressPercent}"
                                 Maximum="100"
                                 Height="22"
                                 Style="{StaticResource GameProgressBar}"/>
                    <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:F0}%}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Style="{DynamicResource BaseTextBlockStyle}"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </Grid>

                <TextBlock Text="{Binding DetailMessage}"
                           Style="{DynamicResource BaseTextBlockStyle}"
                           Foreground="{DynamicResource TextBrush}"
                           Opacity="0.65"
                           FontSize="11"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource ControlBackgroundBrush}"
                Padding="16,12">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{DynamicResource LOCPlayAch_Button_Cancel}"
                        Click="CancelButton_Click"
                    Style="{StaticResource ProgressDialogButtonStyle}"
                        Margin="0,0,8,0"
                        Visibility="{Binding ShowCancelButton, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Content="Close"
                        Click="CloseButton_Click"
                    Style="{StaticResource ProgressDialogButtonStyle}"
                        Visibility="{Binding ShowCloseButton, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>